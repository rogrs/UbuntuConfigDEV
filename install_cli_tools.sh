#!/bin/bash

sudo apt install powerman -y &&
sudo npm install -g composer-cli &&
sudo npm install -g composer-rest-server &&
sudo npm install -g generator-hyperledger-composer &&
sudo npm install -g yo
